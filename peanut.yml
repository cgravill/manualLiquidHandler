apiversion: v2
kind: package
name: github.com/antha-lang/manualLiquidHandler
version: 0.0.1
build:
  morelines:
  - RUN apt-get update && apt-get install -y pkg-config libglpk-dev libopencv-dev
deploy:
  exposeports:
  - 50051
modules:
  golang:
    deploypackage: github.com/antha-lang/manualLiquidHandler/server
    testpackages:
    - ~vendor
    dependson:
    - github.com/antha-lang/antha
